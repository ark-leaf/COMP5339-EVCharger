# Task 1/2/3 runtime. Python 3.12 is the tested interpreter.
# Includes the spatial IO engine explicitly; no system GDAL setup is needed
# when compatible binary wheels are available.
numpy==1.26.4
pandas==2.3.3
geopandas==1.1.4
shapely==2.1.2
pyogrio==0.13.0
pyproj==3.8.0
requests==2.32.2
